package pseudoCitation;

public class AnalyticDriver {

	
	/**
	 * Driver function
	 * - read panel members from file
	 * - read name, year of conference
	 * - add/lookup each member in the citation graph
	 * - add/lookup each member in the collaboration graph
	 * - read in papers from file
	 * - lookup each paper in the data and get authors
	 * - add/lookup each member in the citation graph
	 * - add/lookup each member in the collaboration graph
	 * - get citation distance between each panel member and each author
	 * - get collaboration distance between each panel member and each author
	 */
}
